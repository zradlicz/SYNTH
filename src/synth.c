#include <stdio.h>
#include <stdint.h>
#include "math.h"
#include "synth.h"

uint16_t multiply(uint16_t a, uint16_t b) {
    return a * b;
}

uint16_t add_numbers(uint16_t a, uint16_t b) {
    return a + b;
}